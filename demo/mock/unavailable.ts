
/** Message for actions that need a real Pi Web server. */
export function demoOnlyMessage(): string {
  return "This is a static demo, so this action needs a real Pi Web server. Run npx @agegr/pi-web@latest to try it for real.";
}
